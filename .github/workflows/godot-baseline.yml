name: Godot Baseline

on:
  push:
    branches:
      - main
      - "foundation/**"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: Import, parse, and smoke test
    runs-on: ubuntu-latest
    timeout-minutes: 10

    env:
      GODOT_VERSION: "4.6.3"
      GODOT_BIN: "Godot_v4.6.3-stable_linux.x86_64"

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Download Godot
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --location --retry 3 \
            "https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}-stable/${GODOT_BIN}.zip" \
            --output godot.zip
          unzip -q godot.zip
          chmod +x "${GODOT_BIN}"

      - name: Import project and parse resources
        shell: bash
        run: |
          set -euo pipefail
          "./${GODOT_BIN}" --headless --editor --path . --quit 2>&1 | tee godot-import.log

      - name: Run baseline smoke test
        shell: bash
        run: |
          set -o pipefail
          "./${GODOT_BIN}" --headless --path . --script res://tests/baseline_smoke_test.gd 2>&1 | tee godot-baseline.log

      - name: Run save service smoke test
        shell: bash
        run: |
          set -o pipefail
          "./${GODOT_BIN}" --headless --path . --script res://tests/save_service_smoke_test.gd 2>&1 | tee godot-save-service.log

      - name: Run input smoke test
        shell: bash
        run: |
          set -o pipefail
          "./${GODOT_BIN}" --headless --path . --script res://tests/input_contract_smoke_test.gd 2>&1 | tee godot-input-contract.log

      - name: Run portrait layout smoke test
        shell: bash
        run: |
          set -o pipefail
          "./${GODOT_BIN}" --headless --path . --script res://tests/portrait_layout_smoke_test.gd 2>&1 | tee godot-portrait-layout.log

      - name: Run feedback contract smoke test
        shell: bash
        run: |
          set -o pipefail
          "./${GODOT_BIN}" --headless --path . --script res://tests/feedback_contract_smoke_test.gd 2>&1 | tee godot-feedback-contract.log

      - name: Run development diagnostics smoke test
        shell: bash
        run: |
          set -o pipefail
          "./${GODOT_BIN}" --headless --path . --script res://tests/development_diagnostics_smoke_test.gd 2>&1 | tee godot-development-diagnostics.log

      - name: Reject ObjectDB teardown leaks
        shell: bash
        run: |
          set -euo pipefail
          if grep -H "ObjectDB instances leaked at exit" godot-*.log; then
            echo "::error::Godot reported leaked ObjectDB instances during test teardown."
            exit 1
          fi
          echo "No ObjectDB teardown leaks detected."

      - name: Upload validation logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: godot-baseline-logs
          path: |
            godot-import.log
            godot-baseline.log
            godot-save-service.log
            godot-input-contract.log
            godot-portrait-layout.log
            godot-feedback-contract.log
            godot-development-diagnostics.log
          if-no-files-found: warn
          retention-days: 7
